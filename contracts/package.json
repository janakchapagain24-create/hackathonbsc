{
  "name": "hackathonbsc-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for BSC trading agent",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bscTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bsc",
    "test": "hardhat test",
    "verify:bsc": "hardhat verify --network bsc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "ethers": "^6.7.0",
    "dotenv": "^16.3.1"
  }
}
